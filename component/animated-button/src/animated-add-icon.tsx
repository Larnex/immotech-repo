import React from 'react';
import { Animated } from 'react-native';
import { AddIcon } from 'native-x-icon';

export class AnimatedAddIcon extends React.Component {
    render() {
        return <AddIcon />;
    }
}

// export Animated.createAnimatedComponent(AnimatedAddIcon);