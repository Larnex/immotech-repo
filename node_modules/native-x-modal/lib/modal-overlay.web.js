import { Tappable } from 'native-x-tappable';
import { COLOR, useTheme } from 'native-x-theme';
import React from 'react';
import { View } from 'react-native';
import { styles as s } from 'tachyons-react-native';
const styles = {
    container: [s.flex, s.w100, s.h100],
};
export function ModalOverlay({ onTap }) {
    const { getBackgroundColor } = useTheme();
    const backgroundColor = getBackgroundColor?.(COLOR.SECONDARY);
    return (React.createElement(Tappable, { style: styles.container, onTap: onTap },
        React.createElement(View, { style: [styles.container, backgroundColor] })));
}
