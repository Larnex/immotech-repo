import { CloseIcon } from 'native-x-icon';
import { Stack } from 'native-x-stack';
import { Tappable } from 'native-x-tappable';
import { COLOR } from 'native-x-theme';
import React, { useCallback } from 'react';
import { View } from 'react-native';
import { styles as s } from 'tachyons-react-native';
const styles = {
    container: [s.absolute, s.right0, s.top0],
};
export function ModalCloseButton({ onTap }) {
    const onClose = useCallback(() => {
        onTap?.();
    }, [onTap]);
    return (React.createElement(View, { style: styles.container },
        React.createElement(Tappable, { onTap: onClose },
            React.createElement(Stack, { padding: 'normal' },
                React.createElement(Stack, { height: 27, width: 27, alignCenter: true, alignMiddle: true, opacity: 'medium' },
                    React.createElement(CloseIcon, { color: COLOR.TERTIARY }))))));
}
