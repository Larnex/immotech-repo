import { Stack } from 'native-x-stack';
import { COLOR } from 'native-x-theme';
import React from 'react';
import { Platform } from 'react-native';
import RNModal from 'react-native-modal';
import { styles as s } from 'tachyons-react-native';
import { ModalCloseButton } from './modal-close-button';
import { ModalOverlay } from './modal-overlay';
const styles = {
    container: [s.selfCenter],
};
export function Modal({ children, visible, onClose, width, showClose, ...props }) {
    return (React.createElement(RNModal, { coverScreen: true, isVisible: visible, onModalHide: onClose, onBackdropPress: onClose, backdropOpacity: Platform.OS !== 'web' ? 1 : undefined, useNativeDriver: Platform.OS !== 'web', useNativeDriverForBackdrop: Platform.OS !== 'web', avoidKeyboard: true, customBackdrop: React.createElement(ModalOverlay, { onTap: Platform.OS == 'web' ? onClose : undefined }) },
        React.createElement(Stack, Object.assign({ alignCenter: true, alignMiddle: true, backgroundColor: COLOR.PRIMARY, borderRadius: 'large', width: width, style: styles.container }, props),
            children,
            showClose && React.createElement(ModalCloseButton, { onTap: onClose }))));
}
