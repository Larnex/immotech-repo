import React from 'react';
import { ActivityIndicator } from 'react-native';
import { COLOR, useTheme } from 'native-x-theme';
export function Spinner({ animating = true, color = COLOR.SECONDARY, size = 'normal', }) {
    const { getColor } = useTheme();
    const scale = size === 'x-large'
        ? { transform: [{ scale: 1.25 }] }
        : size === 'small'
            ? { transform: [{ scale: 0.7 }] }
            : size === 'x-small'
                ? { transform: [{ scale: 0.5 }] }
                : undefined;
    const indicatorSize = size === 'x-large' ? 'large' : size === 'large' ? size : 'small';
    return (React.createElement(ActivityIndicator, { size: indicatorSize, animating: animating, color: getColor?.(color), style: scale }));
}
