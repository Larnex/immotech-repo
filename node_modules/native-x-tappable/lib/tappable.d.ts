import { ReactNode } from 'react';
import { ViewStyle } from 'react-native';
declare type BaseType = {
    children?: ReactNode | ReactNode[] | null;
    disabled?: boolean;
    feedback?: boolean;
    style?: ViewStyle;
};
declare type PropsWithData<TData> = {
    data: TData;
    onTap?: (data: TData) => void;
} & BaseType;
declare type PropsWithoutData = {
    onTap?: () => void;
} & BaseType;
export declare type TappableProps<T> = PropsWithData<T> | PropsWithoutData;
export declare function Tappable<TData>(props: TappableProps<TData>): JSX.Element;
export {};
