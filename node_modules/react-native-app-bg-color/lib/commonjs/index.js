"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getBackgroundColor = getBackgroundColor;
exports.setBackgroundColor = setBackgroundColor;

var _reactNative = require("react-native");

const LINKING_ERROR = `The package 'react-native-app-bg-color' doesn't seem to be linked. Make sure: \n\n` + _reactNative.Platform.select({
  ios: "- You have run 'pod install'\n",
  default: ''
}) + '- You rebuilt the app after installing the package\n' + '- You are not using Expo managed workflow\n';
const AppBgColor = _reactNative.NativeModules.AppBgColor ? _reactNative.NativeModules.AppBgColor : new Proxy({}, {
  get() {
    throw new Error(LINKING_ERROR);
  }

});

function setBackgroundColor(color) {
  return AppBgColor.setBackgroundColor(color);
}

function getBackgroundColor(callback) {
  return AppBgColor.getBackgroundColor(callback);
}
//# sourceMappingURL=index.js.map