/// <reference types="react" />
import { IconSize } from './icon-size';
export interface BusIconProps {
    color?: string;
    size?: IconSize;
}
export declare function BusIcon({ size, color }: BusIconProps): JSX.Element;
