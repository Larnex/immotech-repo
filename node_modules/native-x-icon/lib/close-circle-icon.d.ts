/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CloseCircleIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CloseCircleIcon({ size, color, }: CloseCircleIconProps): JSX.Element;
