/// <reference types="react" />
import { IconSize } from './icon-size';
export interface PizzaIconProps {
    color?: string;
    size?: IconSize;
}
export declare function PizzaIcon({ size, color }: PizzaIconProps): JSX.Element;
