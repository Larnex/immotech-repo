/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CogIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CogIcon({ size, color }: CogIconProps): JSX.Element;
