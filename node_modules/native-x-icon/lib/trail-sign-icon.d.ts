/// <reference types="react" />
import { IconSize } from './icon-size';
export interface TrailSignIconProps {
    color?: string;
    size?: IconSize;
}
export declare function TrailSignIcon({ size, color }: TrailSignIconProps): JSX.Element;
