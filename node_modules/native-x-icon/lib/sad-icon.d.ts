/// <reference types="react" />
import { IconSize } from './icon-size';
export interface SadIconProps {
    color?: string;
    size?: IconSize;
}
export declare function SadIcon({ size, color }: SadIconProps): JSX.Element;
