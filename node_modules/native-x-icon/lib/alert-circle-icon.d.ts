/// <reference types="react" />
import { IconSize } from './icon-size';
export interface AlertCircleIconProps {
    color?: string;
    size?: IconSize;
}
export declare function AlertCircleIcon({ size, color, }: AlertCircleIconProps): JSX.Element;
