/// <reference types="react" />
import { IconSize } from './icon-size';
export interface ScanIconProps {
    color?: string;
    size?: IconSize;
}
export declare function ScanIcon({ size, color }: ScanIconProps): JSX.Element;
