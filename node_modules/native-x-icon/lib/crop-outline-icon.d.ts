/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CropOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CropOutlineIcon({ size, color, }: CropOutlineIconProps): JSX.Element;
