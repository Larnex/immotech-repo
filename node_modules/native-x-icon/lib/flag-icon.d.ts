/// <reference types="react" />
import { IconSize } from './icon-size';
export interface FlagIconProps {
    color?: string;
    size?: IconSize;
}
export declare function FlagIcon({ size, color }: FlagIconProps): JSX.Element;
