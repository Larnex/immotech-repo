/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CameraReverseIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CameraReverseIcon({ size, color, }: CameraReverseIconProps): JSX.Element;
