/// <reference types="react" />
import { IconSize } from './icon-size';
export interface BookIconProps {
    color?: string;
    size?: IconSize;
}
export declare function BookIcon({ size, color }: BookIconProps): JSX.Element;
