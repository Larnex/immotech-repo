import { COLOR, useTheme } from 'native-x-theme';
import * as React from 'react';
import Svg, { Path } from 'react-native-svg';
import { getIconSize } from './icon-size';
export function SchoolOutlineIcon({ size = 'normal', color = COLOR.SECONDARY, }) {
    const { getColor } = useTheme();
    const contentColor = getColor?.(color);
    const iconSize = getIconSize(size);
    return (React.createElement(Svg, { width: iconSize, height: iconSize, viewBox: '0 0 24 24', fill: 'none' },
        React.createElement(Path, { d: 'M1.5 9L12 3L22.5 9L12 15L1.5 9Z', stroke: contentColor, strokeLinecap: 'round', strokeLinejoin: 'round' }),
        React.createElement(Path, { d: 'M5.25 11.25V17.25L12 21L18.75 17.25V11.25', stroke: contentColor, strokeLinecap: 'round', strokeLinejoin: 'round' }),
        React.createElement(Path, { d: 'M22.5 17.25V9', stroke: contentColor, strokeLinecap: 'round', strokeLinejoin: 'round' }),
        React.createElement(Path, { d: 'M12 15V21', stroke: contentColor, strokeLinecap: 'round', strokeLinejoin: 'round' })));
}
