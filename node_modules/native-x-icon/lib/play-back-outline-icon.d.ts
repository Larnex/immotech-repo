/// <reference types="react" />
import { IconSize } from './icon-size';
export interface PlayBackOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function PlayBackOutlineIcon({ size, color, }: PlayBackOutlineIconProps): JSX.Element;
