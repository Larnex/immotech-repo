/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CopyIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CopyIcon({ size, color }: CopyIconProps): JSX.Element;
