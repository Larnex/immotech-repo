/// <reference types="react" />
import { IconSize } from './icon-size';
export interface LogoDribbbleIconProps {
    color?: string;
    size?: IconSize;
}
export declare function LogoDribbbleIcon({ size, color, }: LogoDribbbleIconProps): JSX.Element;
