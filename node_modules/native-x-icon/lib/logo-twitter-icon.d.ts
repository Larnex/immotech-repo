/// <reference types="react" />
import { IconSize } from './icon-size';
export interface LogoTwitterIconProps {
    color?: string;
    size?: IconSize;
}
export declare function LogoTwitterIcon({ size, color, }: LogoTwitterIconProps): JSX.Element;
