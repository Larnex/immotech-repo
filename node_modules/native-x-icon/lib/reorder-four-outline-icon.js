import { COLOR, useTheme } from 'native-x-theme';
import * as React from 'react';
import Svg, { Path } from 'react-native-svg';
import { getIconSize } from './icon-size';
export function ReorderFourOutlineIcon({ size = 'normal', color = COLOR.SECONDARY, }) {
    const { getColor } = useTheme();
    const contentColor = getColor?.(color);
    const iconSize = getIconSize(size);
    return (React.createElement(Svg, { width: iconSize, height: iconSize, viewBox: '0 0 24 24', fill: 'none' },
        React.createElement(Path, { d: 'M4.5 14.25H19.5', stroke: contentColor, strokeLinecap: 'round', strokeLinejoin: 'round' }),
        React.createElement(Path, { d: 'M4.5 9.75H19.5', stroke: contentColor, strokeLinecap: 'round', strokeLinejoin: 'round' }),
        React.createElement(Path, { d: 'M4.5 5.25H19.5', stroke: contentColor, strokeLinecap: 'round', strokeLinejoin: 'round' }),
        React.createElement(Path, { d: 'M4.5 18.75H19.5', stroke: contentColor, strokeLinecap: 'round', strokeLinejoin: 'round' })));
}
