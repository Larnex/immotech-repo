/// <reference types="react" />
import { IconSize } from './icon-size';
export interface PlayBackIconProps {
    color?: string;
    size?: IconSize;
}
export declare function PlayBackIcon({ size, color }: PlayBackIconProps): JSX.Element;
