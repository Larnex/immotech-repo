/// <reference types="react" />
import { IconSize } from './icon-size';
export interface RestaurantIconProps {
    color?: string;
    size?: IconSize;
}
export declare function RestaurantIcon({ size, color }: RestaurantIconProps): JSX.Element;
