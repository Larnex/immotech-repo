/// <reference types="react" />
import { IconSize } from './icon-size';
export interface MusicalNoteIconProps {
    color?: string;
    size?: IconSize;
}
export declare function MusicalNoteIcon({ size, color, }: MusicalNoteIconProps): JSX.Element;
