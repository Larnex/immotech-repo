/// <reference types="react" />
import { IconSize } from './icon-size';
export interface FolderOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function FolderOutlineIcon({ size, color, }: FolderOutlineIconProps): JSX.Element;
