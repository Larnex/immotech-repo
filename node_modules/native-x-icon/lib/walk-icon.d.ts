/// <reference types="react" />
import { IconSize } from './icon-size';
export interface WalkIconProps {
    color?: string;
    size?: IconSize;
}
export declare function WalkIcon({ size, color }: WalkIconProps): JSX.Element;
