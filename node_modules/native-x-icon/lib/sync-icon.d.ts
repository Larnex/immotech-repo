/// <reference types="react" />
import { IconSize } from './icon-size';
export interface SyncIconProps {
    color?: string;
    size?: IconSize;
}
export declare function SyncIcon({ size, color }: SyncIconProps): JSX.Element;
