/// <reference types="react" />
import { IconSize } from './icon-size';
export interface PlayIconProps {
    color?: string;
    size?: IconSize;
}
export declare function PlayIcon({ size, color }: PlayIconProps): JSX.Element;
