/// <reference types="react" />
import { IconSize } from './icon-size';
export interface SaveIconProps {
    color?: string;
    size?: IconSize;
}
export declare function SaveIcon({ size, color }: SaveIconProps): JSX.Element;
