/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CheckmarkIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CheckmarkIcon({ size, color }: CheckmarkIconProps): JSX.Element;
