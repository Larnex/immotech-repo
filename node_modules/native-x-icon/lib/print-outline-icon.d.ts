/// <reference types="react" />
import { IconSize } from './icon-size';
export interface PrintOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function PrintOutlineIcon({ size, color, }: PrintOutlineIconProps): JSX.Element;
