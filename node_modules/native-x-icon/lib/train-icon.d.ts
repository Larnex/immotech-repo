/// <reference types="react" />
import { IconSize } from './icon-size';
export interface TrainIconProps {
    color?: string;
    size?: IconSize;
}
export declare function TrainIcon({ size, color }: TrainIconProps): JSX.Element;
