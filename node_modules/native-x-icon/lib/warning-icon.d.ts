/// <reference types="react" />
import { IconSize } from './icon-size';
export interface WarningIconProps {
    color?: string;
    size?: IconSize;
}
export declare function WarningIcon({ size, color }: WarningIconProps): JSX.Element;
