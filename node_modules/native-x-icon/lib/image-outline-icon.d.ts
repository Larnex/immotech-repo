/// <reference types="react" />
import { IconSize } from './icon-size';
export interface ImageOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function ImageOutlineIcon({ size, color, }: ImageOutlineIconProps): JSX.Element;
