/// <reference types="react" />
import { IconSize } from './icon-size';
export interface EarthIconProps {
    color?: string;
    size?: IconSize;
}
export declare function EarthIcon({ size, color }: EarthIconProps): JSX.Element;
