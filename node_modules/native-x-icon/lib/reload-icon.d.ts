/// <reference types="react" />
import { IconSize } from './icon-size';
export interface ReloadIconProps {
    color?: string;
    size?: IconSize;
}
export declare function ReloadIcon({ size, color }: ReloadIconProps): JSX.Element;
