/// <reference types="react" />
import { IconSize } from './icon-size';
export interface PizzaOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function PizzaOutlineIcon({ size, color, }: PizzaOutlineIconProps): JSX.Element;
