/// <reference types="react" />
import { IconSize } from './icon-size';
export interface OpenIconProps {
    color?: string;
    size?: IconSize;
}
export declare function OpenIcon({ size, color }: OpenIconProps): JSX.Element;
