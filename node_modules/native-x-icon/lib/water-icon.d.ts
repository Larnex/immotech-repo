/// <reference types="react" />
import { IconSize } from './icon-size';
export interface WaterIconProps {
    color?: string;
    size?: IconSize;
}
export declare function WaterIcon({ size, color }: WaterIconProps): JSX.Element;
