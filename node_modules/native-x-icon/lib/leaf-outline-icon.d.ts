/// <reference types="react" />
import { IconSize } from './icon-size';
export interface LeafOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function LeafOutlineIcon({ size, color, }: LeafOutlineIconProps): JSX.Element;
