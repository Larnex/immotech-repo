/// <reference types="react" />
import { IconSize } from './icon-size';
export interface MicIconProps {
    color?: string;
    size?: IconSize;
}
export declare function MicIcon({ size, color }: MicIconProps): JSX.Element;
