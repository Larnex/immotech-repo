/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CloudUploadIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CloudUploadIcon({ size, color, }: CloudUploadIconProps): JSX.Element;
