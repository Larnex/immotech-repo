/// <reference types="react" />
import { IconSize } from './icon-size';
export interface NewspaperIconProps {
    color?: string;
    size?: IconSize;
}
export declare function NewspaperIcon({ size, color }: NewspaperIconProps): JSX.Element;
