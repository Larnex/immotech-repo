/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CalendarIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CalendarIcon({ size, color }: CalendarIconProps): JSX.Element;
