/// <reference types="react" />
import { IconSize } from './icon-size';
export interface EarIconProps {
    color?: string;
    size?: IconSize;
}
export declare function EarIcon({ size, color }: EarIconProps): JSX.Element;
