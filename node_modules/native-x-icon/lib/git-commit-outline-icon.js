import { COLOR, useTheme } from 'native-x-theme';
import * as React from 'react';
import Svg, { Path } from 'react-native-svg';
import { getIconSize } from './icon-size';
export function GitCommitOutlineIcon({ size = 'normal', color = COLOR.SECONDARY, }) {
    const { getColor } = useTheme();
    const contentColor = getColor?.(color);
    const iconSize = getIconSize(size);
    return (React.createElement(Svg, { width: iconSize, height: iconSize, viewBox: '0 0 24 24', fill: 'none' },
        React.createElement(Path, { d: 'M12 16.5C14.4853 16.5 16.5 14.4853 16.5 12C16.5 9.51472 14.4853 7.5 12 7.5C9.51472 7.5 7.5 9.51472 7.5 12C7.5 14.4853 9.51472 16.5 12 16.5Z', stroke: contentColor, strokeLinecap: 'round', strokeLinejoin: 'round' }),
        React.createElement(Path, { d: 'M7.5 12H2.25', stroke: contentColor, strokeLinecap: 'round', strokeLinejoin: 'round' }),
        React.createElement(Path, { d: 'M21.75 12H16.5', stroke: contentColor, strokeLinecap: 'round', strokeLinejoin: 'round' })));
}
