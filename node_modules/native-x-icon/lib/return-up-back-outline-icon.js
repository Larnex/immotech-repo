import { COLOR, useTheme } from 'native-x-theme';
import * as React from 'react';
import Svg, { Path } from 'react-native-svg';
import { getIconSize } from './icon-size';
export function ReturnUpBackOutlineIcon({ size = 'normal', color = COLOR.SECONDARY, }) {
    const { getColor } = useTheme();
    const contentColor = getColor?.(color);
    const iconSize = getIconSize(size);
    return (React.createElement(Svg, { width: iconSize, height: iconSize, viewBox: '0 0 24 24', fill: 'none' },
        React.createElement(Path, { d: 'M5.25 7.5L2.25 10.5L5.25 13.5', stroke: contentColor, strokeLinecap: 'round', strokeLinejoin: 'round' }),
        React.createElement(Path, { d: 'M3 10.5H16.7812C19.5356 10.5 21.75 12.8123 21.75 15.5625V16.5', stroke: contentColor, strokeLinecap: 'round', strokeLinejoin: 'round' })));
}
