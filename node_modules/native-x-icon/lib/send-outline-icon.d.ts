/// <reference types="react" />
import { IconSize } from './icon-size';
export interface SendOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function SendOutlineIcon({ size, color, }: SendOutlineIconProps): JSX.Element;
