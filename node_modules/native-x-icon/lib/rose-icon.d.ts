/// <reference types="react" />
import { IconSize } from './icon-size';
export interface RoseIconProps {
    color?: string;
    size?: IconSize;
}
export declare function RoseIcon({ size, color }: RoseIconProps): JSX.Element;
