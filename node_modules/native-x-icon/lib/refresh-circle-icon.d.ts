/// <reference types="react" />
import { IconSize } from './icon-size';
export interface RefreshCircleIconProps {
    color?: string;
    size?: IconSize;
}
export declare function RefreshCircleIcon({ size, color, }: RefreshCircleIconProps): JSX.Element;
