/// <reference types="react" />
import { IconSize } from './icon-size';
export interface PowerIconProps {
    color?: string;
    size?: IconSize;
}
export declare function PowerIcon({ size, color }: PowerIconProps): JSX.Element;
