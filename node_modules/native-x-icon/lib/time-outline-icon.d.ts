/// <reference types="react" />
import { IconSize } from './icon-size';
export interface TimeOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function TimeOutlineIcon({ size, color, }: TimeOutlineIconProps): JSX.Element;
