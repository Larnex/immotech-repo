/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CallIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CallIcon({ size, color }: CallIconProps): JSX.Element;
