/// <reference types="react" />
import { IconSize } from './icon-size';
export interface PintOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function PintOutlineIcon({ size, color, }: PintOutlineIconProps): JSX.Element;
