/// <reference types="react" />
import { IconSize } from './icon-size';
export interface PlayForwardIconProps {
    color?: string;
    size?: IconSize;
}
export declare function PlayForwardIcon({ size, color, }: PlayForwardIconProps): JSX.Element;
