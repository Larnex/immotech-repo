/// <reference types="react" />
import { IconSize } from './icon-size';
export interface ImageIconProps {
    color?: string;
    size?: IconSize;
}
export declare function ImageIcon({ size, color }: ImageIconProps): JSX.Element;
