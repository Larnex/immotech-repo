/// <reference types="react" />
import { IconSize } from './icon-size';
export interface TimerIconProps {
    color?: string;
    size?: IconSize;
}
export declare function TimerIcon({ size, color }: TimerIconProps): JSX.Element;
