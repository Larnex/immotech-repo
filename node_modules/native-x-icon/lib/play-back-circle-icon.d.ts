/// <reference types="react" />
import { IconSize } from './icon-size';
export interface PlayBackCircleIconProps {
    color?: string;
    size?: IconSize;
}
export declare function PlayBackCircleIcon({ size, color, }: PlayBackCircleIconProps): JSX.Element;
