/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CheckmarkOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CheckmarkOutlineIcon({ size, color, }: CheckmarkOutlineIconProps): JSX.Element;
