/// <reference types="react" />
import { IconSize } from './icon-size';
export interface TicketOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function TicketOutlineIcon({ size, color, }: TicketOutlineIconProps): JSX.Element;
