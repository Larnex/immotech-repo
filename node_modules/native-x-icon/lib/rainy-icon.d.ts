/// <reference types="react" />
import { IconSize } from './icon-size';
export interface RainyIconProps {
    color?: string;
    size?: IconSize;
}
export declare function RainyIcon({ size, color }: RainyIconProps): JSX.Element;
