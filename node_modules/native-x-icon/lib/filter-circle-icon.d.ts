/// <reference types="react" />
import { IconSize } from './icon-size';
export interface FilterCircleIconProps {
    color?: string;
    size?: IconSize;
}
export declare function FilterCircleIcon({ size, color, }: FilterCircleIconProps): JSX.Element;
