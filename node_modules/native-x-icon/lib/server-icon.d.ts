/// <reference types="react" />
import { IconSize } from './icon-size';
export interface ServerIconProps {
    color?: string;
    size?: IconSize;
}
export declare function ServerIcon({ size, color }: ServerIconProps): JSX.Element;
