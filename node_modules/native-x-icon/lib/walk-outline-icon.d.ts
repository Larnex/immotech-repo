/// <reference types="react" />
import { IconSize } from './icon-size';
export interface WalkOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function WalkOutlineIcon({ size, color, }: WalkOutlineIconProps): JSX.Element;
