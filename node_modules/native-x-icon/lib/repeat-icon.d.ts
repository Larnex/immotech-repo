/// <reference types="react" />
import { IconSize } from './icon-size';
export interface RepeatIconProps {
    color?: string;
    size?: IconSize;
}
export declare function RepeatIcon({ size, color }: RepeatIconProps): JSX.Element;
