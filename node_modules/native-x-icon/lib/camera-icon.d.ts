/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CameraIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CameraIcon({ size, color }: CameraIconProps): JSX.Element;
