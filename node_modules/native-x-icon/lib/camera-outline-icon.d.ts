/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CameraOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CameraOutlineIcon({ size, color, }: CameraOutlineIconProps): JSX.Element;
