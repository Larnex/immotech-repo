/// <reference types="react" />
import { IconSize } from './icon-size';
export interface FunnelIconProps {
    color?: string;
    size?: IconSize;
}
export declare function FunnelIcon({ size, color }: FunnelIconProps): JSX.Element;
