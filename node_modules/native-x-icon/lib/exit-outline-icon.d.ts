/// <reference types="react" />
import { IconSize } from './icon-size';
export interface ExitOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function ExitOutlineIcon({ size, color, }: ExitOutlineIconProps): JSX.Element;
