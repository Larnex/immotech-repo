/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CardIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CardIcon({ size, color }: CardIconProps): JSX.Element;
