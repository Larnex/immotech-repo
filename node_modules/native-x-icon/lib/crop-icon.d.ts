/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CropIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CropIcon({ size, color }: CropIconProps): JSX.Element;
