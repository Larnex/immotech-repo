/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CheckboxOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CheckboxOutlineIcon({ size, color, }: CheckboxOutlineIconProps): JSX.Element;
