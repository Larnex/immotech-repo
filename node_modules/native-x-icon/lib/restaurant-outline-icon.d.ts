/// <reference types="react" />
import { IconSize } from './icon-size';
export interface RestaurantOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function RestaurantOutlineIcon({ size, color, }: RestaurantOutlineIconProps): JSX.Element;
