/// <reference types="react" />
import { IconSize } from './icon-size';
export interface HardwareChipIconProps {
    color?: string;
    size?: IconSize;
}
export declare function HardwareChipIcon({ size, color, }: HardwareChipIconProps): JSX.Element;
