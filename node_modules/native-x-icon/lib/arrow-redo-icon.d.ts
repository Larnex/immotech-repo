/// <reference types="react" />
import { IconSize } from './icon-size';
export interface ArrowRedoIconProps {
    color?: string;
    size?: IconSize;
}
export declare function ArrowRedoIcon({ size, color }: ArrowRedoIconProps): JSX.Element;
