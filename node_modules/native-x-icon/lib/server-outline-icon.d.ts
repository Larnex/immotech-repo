/// <reference types="react" />
import { IconSize } from './icon-size';
export interface ServerOutlineIconProps {
    color?: string;
    size?: IconSize;
}
export declare function ServerOutlineIcon({ size, color, }: ServerOutlineIconProps): JSX.Element;
