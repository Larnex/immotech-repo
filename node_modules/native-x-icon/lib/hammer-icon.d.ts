/// <reference types="react" />
import { IconSize } from './icon-size';
export interface HammerIconProps {
    color?: string;
    size?: IconSize;
}
export declare function HammerIcon({ size, color }: HammerIconProps): JSX.Element;
