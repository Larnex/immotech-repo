/// <reference types="react" />
import { IconSize } from './icon-size';
export interface CartIconProps {
    color?: string;
    size?: IconSize;
}
export declare function CartIcon({ size, color }: CartIconProps): JSX.Element;
