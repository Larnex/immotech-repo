/// <reference types="react" />
import { IconSize } from './icon-size';
export interface LogoTableauIconProps {
    color?: string;
    size?: IconSize;
}
export declare function LogoTableauIcon({ size, color, }: LogoTableauIconProps): JSX.Element;
