/// <reference types="react" />
import { IconSize } from './icon-size';
export interface FishIconProps {
    color?: string;
    size?: IconSize;
}
export declare function FishIcon({ size, color }: FishIconProps): JSX.Element;
