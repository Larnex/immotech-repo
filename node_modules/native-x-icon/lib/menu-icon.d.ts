/// <reference types="react" />
import { IconSize } from './icon-size';
export interface MenuIconProps {
    color?: string;
    size?: IconSize;
}
export declare function MenuIcon({ size, color }: MenuIconProps): JSX.Element;
